<?php

use Illuminate\Support\Facades\Route;

// The Laravel application is API-only.
// All API endpoints live in `routes/api.php` and are served under `/api/*`.
// Frontend routes are handled by the React SPA (served by nginx or Vite dev server).

// No web routes or SPA fallbacks are defined here intentionally.

